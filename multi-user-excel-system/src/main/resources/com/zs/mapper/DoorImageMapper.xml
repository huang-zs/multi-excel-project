<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.zs.mapper.DoorImageMapper">

	
	<select id="list" resultType="DoorImage">
		select * from door_image where image_index is not null ORDER BY image_index asc
	</select>
	
</mapper>